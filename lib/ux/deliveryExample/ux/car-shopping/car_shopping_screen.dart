import 'package:flutter/material.dart';

class CarShoppingScreen extends StatelessWidget {
  const CarShoppingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Car Shopping')),
    );
  }
}
